import React from 'react';
import { ArrowRight } from 'lucide-react';
import { SitePageShell } from './SitePageShell';
import { useAppImages } from '../context/ImageContext';
import { COLLECTION_ITEMS, PREMIUM_PRODUCT_IMAGE_FALLBACKS } from '../constants/data';

export const CollectionLandingPage: React.FC<{onAdminClick?:()=>void;openLegal?:(d:any)=>void}> = ({onAdminClick,openLegal}) => { const {collectionItems}=useAppImages(); const items=collectionItems?.length?collectionItems:COLLECTION_ITEMS; return <SitePageShell title="2026 / 2027 kadın comfort koleksiyonu" eyebrow="KOLEKSİYONLAR" intro="Mağaza rafınız için dengeli model, renk ve konfor seçeneklerinden oluşan yeni sezon seçkisi." activePath="/koleksiyonlar" onAdminClick={onAdminClick} legal={{open:openLegal!}}><section className="max-w-7xl mx-auto px-5 sm:px-8 lg:px-10 py-12 sm:py-20"><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">{items.map(p=><a href={`/urunler/${encodeURIComponent(p.id)}`} key={p.id} className="group bg-white rounded-[2rem] border border-slate-200 overflow-hidden"><div className="aspect-[4/3] overflow-hidden bg-slate-100"><img src={p.image||PREMIUM_PRODUCT_IMAGE_FALLBACKS[p.id]} className="w-full h-full object-cover group-hover:scale-[1.04] transition duration-700"/></div><div className="p-6"><div className="text-[10px] uppercase tracking-[.18em] text-[#8b6a2b] font-bold">{p.category}</div><h2 className="mt-2 text-xl font-serif-luxury font-bold text-[#102f59]">{p.name}</h2><p className="mt-2 text-sm text-slate-600 line-clamp-2">{p.tagline}</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-[#102f59]">Ürün detayına git <ArrowRight className="w-4 h-4"/></span></div></a>)}</div></section></SitePageShell> };
