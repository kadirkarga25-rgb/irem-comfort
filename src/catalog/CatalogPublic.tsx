import React,{useEffect,useMemo,useState} from 'react';
import {Search,Heart,BookOpen,FileHeart,Download,Share2,ChevronLeft,ChevronRight,Maximize2,Minimize2,ZoomIn,ZoomOut,Layers3,ArrowLeft} from 'lucide-react';
import {Catalog,getCatalog,listCatalogs} from './db';
import {CatalogCard,CatalogHeader,CatalogFooter,CollectionCard} from './components';
import {CatalogLink,useCatalogQuery,navigateCatalog} from './router';
import {renderPdfPage} from './pdf';

export function CatalogHome(){const[catalogs,setCatalogs]=useState<Catalog[]>([]);const[q,setQ]=useState('');useEffect(()=>{listCatalogs().then(setCatalogs)},[]);const latest=Math.max(...catalogs.map(c=>Number(c.year)||0),0);const groups=useMemo(()=>{const m=new Map<string,Catalog[]>();catalogs.forEach(c=>m.set(c.year||'Diğer',[...(m.get(c.year||'Diğer')||[]),c]));return [...m.entries()].sort((a,b)=>Number(b[0])-Number(a[0]))},[catalogs]);const hits=useMemo(()=>{const x=q.trim().toLocaleLowerCase('tr-TR');if(!x)return [];return catalogs.flatMap(c=>{const a=[c.title,c.year,c.season,c.description,...(c.tags||[]),...(c.collections||[]).map(v=>v.name)].join(' ').toLocaleLowerCase('tr-TR');return a.includes(x)?[{c,page:1,title:c.title}]:[]}).slice(0,80)},[catalogs,q]);return <div className="ic-catalog"><CatalogHeader/><main><section className="hero"><div className="container hero-grid"><div><div className="eyebrow">İREM COMFORT · DİJİTAL ARŞİV</div><h1 className="serif">Katalog<br/>Arşivi</h1><p>İrem Comfort kataloglarını yıllara, sezonlara ve koleksiyonlara göre keşfedin.</p><div className="hero-actions"><a className="btn btn-primary" href="#arsiv">Arşivi Keşfet →</a><CatalogLink className="btn btn-secondary" href="/katalog/koleksiyonlar">Koleksiyonlar</CatalogLink></div></div><div className="hero-art"><img src="/katalog-assets/logo-written-light.png" alt="İrem Comfort"/></div></div></section><section className="section" id="arsiv"><div className="container"><div className="section-head"><div><div className="eyebrow">ARŞİV</div><h2 className="serif">Yıllara Göre Kataloglar</h2><p>{latest?'Yeni kataloglar yayınlandıkça arşiv otomatik güncellenir.':'Henüz yayınlanmış katalog bulunmuyor.'}</p></div></div><div className="catalog-search"><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Katalog, koleksiyon veya ürün metni ara…"/></div>{q?<div className="search-results">{hits.length?hits.map((h,i)=><CatalogLink key={i} href={`/katalog/viewer/${h.c.id}?page=${h.page}`} className="search-result"><div><strong>{h.c.title}</strong><span>{h.title}</span></div><small>{h.c.year} · Sayfa {h.page}</small></CatalogLink>):<div className="empty">Aramanızla eşleşen sonuç bulunamadı.</div>}</div>:groups.length?groups.map(([year,items])=><section className="year-section" key={year}><div className="year-heading"><div><span className="year-kicker">{Number(year)===latest?'Güncel Sezon':Number(year)===latest-1?'Geçen Sezon':'Daha Önceki Sezon'}</span><h2 className="serif">{year}</h2></div><span className="year-count">{items.length} katalog</span></div><div className="catalog-grid">{items.map(c=><CatalogCard c={c} key={c.id}/>)}</div></section>):<div className="empty">Henüz katalog eklenmemiş.</div>}</div></section></main><CatalogFooter/></div>}

export function CatalogDetail({id}:{id:string}){const[c,setC]=useState<Catalog>();const[fav,setFav]=useState(false);useEffect(()=>{getCatalog(id).then(x=>{setC(x);setFav(localStorage.getItem(`fav-catalog-${id}`)==='1')})},[id]);if(!c)return <div className="ic-catalog"><CatalogHeader/><main className="section"><div className="container"><div className="empty">Katalog yükleniyor…</div></div></main></div>;const toggle=()=>{const n=!fav;setFav(n);localStorage.setItem(`fav-catalog-${id}`,n?'1':'0')};const share=async()=>{const url=`${location.origin}/katalog/viewer/${id}?page=1`;if(navigator.share)await navigator.share({title:c.title,url});else{await navigator.clipboard.writeText(url);alert('Katalog bağlantısı kopyalandı.')}};const download=()=>{if(!c.pdf)return alert('Bu katalog için PDF indirme verisi bulunmuyor.');const url=URL.createObjectURL(c.pdf),a=document.createElement('a');a.href=url;a.download=`${c.title}.pdf`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)};return <div className="ic-catalog"><CatalogHeader/><main className="section"><div className="container"><div className="detail-hero"><div className="cover detail-cover">{c.cover?<img src={c.cover} alt=""/>:<div className="cover-placeholder"><div><small>İREM COMFORT</small><h2 className="serif">{c.title}</h2></div></div>}</div><div><div className="eyebrow">{c.year} · {c.season}</div><h1 className="serif detail-title">{c.title}</h1><p className="detail-description">{c.description}</p><div className="detail-stats"><span>{c.pages} sayfa</span><span>{c.collections.length} koleksiyon</span>{(c.tags||[]).map(t=><span key={t}>{t}</span>)}</div><div className="hero-actions"><CatalogLink className="btn btn-primary" href={`/katalog/viewer/${c.id}`}>Kataloğu Aç →</CatalogLink><button className="btn btn-secondary" onClick={toggle}><Heart size={17} fill={fav?'currentColor':'none'}/>{fav?'Favorilerde':'Favoriye Ekle'}</button><button className="btn btn-secondary" onClick={share}><Share2 size={17}/> Paylaş</button>{c.pdf&&<button className="btn btn-secondary" onClick={download}><Download size={17}/> PDF İndir</button>}</div></div></div><section style={{marginTop:70}}><div className="section-head"><div><div className="eyebrow">İÇİNDEKİLER</div><h2 className="serif">Koleksiyonlar</h2></div></div><div className="collection-grid">{c.collections.map((x,i)=><CollectionCard key={x.id} catalog={c} col={x} index={i}/>)}</div></section><section style={{marginTop:60}}><div className="section-head"><div><div className="eyebrow">İÇERİK</div><h2 className="serif">İçindekiler</h2></div></div><div className="toc-list">{c.contents.map((x,i)=><CatalogLink href={`/katalog/viewer/${c.id}?page=${x.page}`} key={`${x.title}-${i}`}><span>{x.title}</span><b>{x.page}</b></CatalogLink>)}</div></section></div></main><CatalogFooter/></div>}

export function CatalogCollections(){const[catalogs,setCatalogs]=useState<Catalog[]>([]);useEffect(()=>{listCatalogs().then(setCatalogs)},[]);return <div className="ic-catalog"><CatalogHeader/><main className="section"><div className="container"><div className="eyebrow">ARŞİVİN İÇİNDEN</div><h1 className="serif" style={{fontSize:52}}>Koleksiyonlar</h1><p className="intro-copy">Kataloglardaki tüm koleksiyonları yıl yıl görüntüleyin.</p>{catalogs.map(c=><section className="collection-catalog-group" key={c.id}><div className="collection-catalog-head"><div><span className="eyebrow">{c.year} · {c.season}</span><h2 className="serif">{c.title}</h2></div><span className="year-count">{c.collections.length} koleksiyon</span></div><div className="collection-grid">{c.collections.map((col,i)=><CollectionCard key={col.id} catalog={c} col={col} index={i}/>)}</div></section>)}{!catalogs.length&&<div className="empty">Henüz yayınlanmış katalog veya koleksiyon yok.</div>}</div></main><CatalogFooter/></div>}

export function CatalogFavorites(){const[items,setItems]=useState<{catalog:Catalog;pages:number[];catalogFavorite:boolean}[]>([]);useEffect(()=>{listCatalogs().then(cs=>setItems(cs.map(c=>{let pages:number[]=[];try{pages=JSON.parse(localStorage.getItem(`fav-pages-${c.id}`)||'[]')}catch{};return{catalog:c,pages,catalogFavorite:localStorage.getItem(`fav-catalog-${c.id}`)==='1'}}).filter(x=>x.pages.length||x.catalogFavorite)))},[]);const ci=items.filter(x=>x.catalogFavorite),pi=items.filter(x=>x.pages.length);return <div className="ic-catalog"><CatalogHeader/><main className="section"><div className="container"><div className="eyebrow">KİŞİSEL ARŞİV</div><h1 className="serif" style={{fontSize:52}}>Favorilerim</h1><p className="intro-copy">Kataloglarını ve beğendiğin katalog sayfalarını aynı yerde yönet.</p><section className="favorites-section"><div className="favorites-heading"><div><div className="eyebrow">01</div><h2 className="serif"><FileHeart size={24}/> Katalog Favorileri</h2></div><span>{ci.length} katalog</span></div>{ci.length?<div className="catalog-grid">{ci.map(({catalog})=><CatalogCard key={catalog.id} c={catalog}/>)}</div>:<div className="empty">Henüz katalog favorin yok.</div>}</section><section className="favorites-section"><div className="favorites-heading"><div><div className="eyebrow">02</div><h2 className="serif"><BookOpen size={24}/> Sayfa Favorileri</h2></div><span>{pi.reduce((n,x)=>n+x.pages.length,0)} sayfa</span></div>{pi.length?pi.map(({catalog,pages})=><section className="favorite-group" key={catalog.id}><div className="collection-catalog-head"><div><span className="eyebrow">{catalog.year} · {catalog.season}</span><h3 className="serif">{catalog.title}</h3></div></div><div className="favorite-pages">{pages.map(page=><CatalogLink key={page} href={`/katalog/viewer/${catalog.id}?page=${page}`} className="favorite-page"><img src={catalog.cover||'/katalog-assets/logo-light.png'} alt={`Sayfa ${page}`}/><span>Sayfa {page}</span></CatalogLink>)}</div></section>):<div className="empty">Henüz favori sayfan yok.</div>}</section></div></main><CatalogFooter/></div>}

export function CatalogViewer({id}:{id:string}){const[c,setC]=useState<Catalog>();const [page,setPage]=useState(0),[zoom,setZoom]=useState(1),[full,setFull]=useState(false),[turn,setTurn]=useState(false),[collectionOpen,setCollectionOpen]=useState(false),[fav,setFav]=useState<number[]>([]),[rendered,setRendered]=useState<string>(),[thumbs,setThumbs]=useState<Record<number,string>>({});const q=useCatalogQuery();useEffect(()=>{getCatalog(id).then(x=>{setC(x);try{setFav(JSON.parse(localStorage.getItem(`fav-pages-${id}`)||'[]'))}catch{};const requested=Number(q.get('page'));if(requested>0)setPage(Math.min(requested-1,Math.max(0,(x?.pages||1)-1)))})},[id,q.get('page')]);useEffect(()=>{if(!c?.pdf||c.pageImages?.length)return;let dead=false;renderPdfPage(c.pdf,page+1,1).then(x=>!dead&&setRendered(x)).catch(()=>{});return()=>{dead=true}},[c,page]);useEffect(()=>{if(!c?.pdf||c.pageImages?.length)return;let dead=false;(async()=>{const n:any={};for(let i=1;i<=c.pages;i++){if(dead)return;try{n[i]=await renderPdfPage(c.pdf!,i,.18)}catch{break}setThumbs({...n})}})();return()=>{dead=true}},[c]);if(!c)return <div className="ic-catalog"><div className="viewer-loading"><div className="loader-box"><img src="/katalog-assets/logo-light.png" style={{width:170}} alt="İrem Comfort"/><p>KATALOG HAZIRLANIYOR</p></div></div></div>;const go=(p:number)=>setPage(Math.max(0,Math.min(c.pages-1,p)));const next=()=>{if(page<c.pages-1){setTurn(true);setTimeout(()=>{setPage(p=>p+1);setTurn(false)},180)}};const prev=()=>{if(page>0){setTurn(true);setTimeout(()=>{setPage(p=>p-1);setTurn(false)},180)}};const toggleFav=()=>{const n=fav.includes(page+1)?fav.filter(x=>x!==page+1):[...fav,page+1].sort((a,b)=>a-b);setFav(n);localStorage.setItem(`fav-pages-${id}`,JSON.stringify(n))};const share=async()=>{const url=`${location.origin}/katalog/viewer/${id}?page=${page+1}`;if(navigator.share)await navigator.share({title:c.title,url});else{await navigator.clipboard.writeText(url);alert('Bağlantı kopyalandı.')}};const download=()=>{if(!c.pdf)return;const u=URL.createObjectURL(c.pdf),a=document.createElement('a');a.href=u;a.download=`${c.title}.pdf`;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)};const img=c.pageImages?.[page]||rendered||c.cover;const current=c.collections.find(x=>page+1>=x.startPage&&page+1<=x.endPage);return <div className={`ic-catalog viewer-shell ${full?'fullscreen':''}`}><div className="viewer-top"><CatalogLink href={`/katalog/${id}`}><img src="/katalog-assets/logo-written-light.png" alt="İrem Comfort"/></CatalogLink><div className="viewer-title"><strong>{c.title}</strong><span>{c.year} · {c.season}</span></div><div className="control-group"><button className="icon-btn viewer-btn" onClick={toggleFav}><Heart size={18} fill={fav.includes(page+1)?'currentColor':'none'}/></button><button className="icon-btn viewer-btn" onClick={share}><Share2 size={18}/></button>{c.pdf&&<button className="icon-btn viewer-btn" onClick={download}><Download size={18}/></button>}<button className="icon-btn viewer-btn" onClick={()=>setZoom(z=>Math.max(.7,z-.1))}><ZoomOut size={18}/></button><button className="icon-btn viewer-btn" onClick={()=>setZoom(z=>Math.min(1.8,z+.1))}><ZoomIn size={18}/></button><button className="icon-btn viewer-btn" onClick={()=>setCollectionOpen(v=>!v)}><Layers3 size={18}/></button><button className="icon-btn viewer-btn" onClick={()=>{if(document.fullscreenElement)document.exitFullscreen();else document.documentElement.requestFullscreen().catch(()=>{});setFull(v=>!v)}}>{full?<Minimize2 size={18}/>:<Maximize2 size={18}/>}</button></div></div><div className="viewer-layout"><main className="viewer-stage"><div className="book-wrap"><div className={`book ${turn?'page-turning':''}`} style={{transform:`scale(${zoom})`,transition:'.2s'}}><div className="page"><img src={img||'/katalog-assets/logo-light.png'} alt={`Sayfa ${page+1}`} draggable={false}/><span className="page-number">{page+1}</span></div></div></div><div className="viewer-bottom"><div className="viewer-controls"><div className="control-group"><button className="icon-btn" onClick={prev} disabled={!page}><ChevronLeft size={20}/></button><button className="icon-btn" onClick={next} disabled={page>=c.pages-1}><ChevronRight size={20}/></button></div><div className="page-status"><strong>{page+1}</strong> / {c.pages}{current&&<span> · {current.name}</span>}</div><CatalogLink href={`/katalog/${id}`} className="icon-btn"><ArrowLeft size={18}/></CatalogLink></div><div className="thumbs">{Array.from({length:c.pages},(_,i)=>{const s=c.pageImages?.[i]||thumbs[i+1];return <button className={`thumb ${i===page?'active':''}`} key={i} onClick={()=>go(i)}>{s?<img src={s} alt={`Sayfa ${i+1}`}/>:<span className="thumb-number">{i+1}</span>}</button>})}</div></div></main><aside className={`viewer-collections ${collectionOpen?'open':''}`}><div className="collection-panel-head"><Layers3 size={17}/><div><strong>Koleksiyonlar</strong><span>{c.collections.length} bölüm</span></div></div><div className="collection-panel-list">{c.collections.map((col,i)=><button key={col.id} className={`viewer-collection ${current?.id===col.id?'active':''}`} onClick={()=>{go(col.startPage-1);setCollectionOpen(false)}}><span className="viewer-collection-number">{String(i+1).padStart(2,'0')}</span><span className="viewer-collection-copy"><strong>{col.name}</strong><small>Sayfa {col.startPage} – {col.endPage}</small></span><ChevronRight size={15}/></button>)}</div></aside></div></div>}
