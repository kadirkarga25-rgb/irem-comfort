import Link from 'next/link'; import type { Catalog } from '../lib/db';
export function CatalogCard({c,basePath='/katalog'}:{c:Catalog;basePath?:string}){return <Link href={`${basePath}/${c.id}`} style={{textDecoration:'none',color:'inherit'}}><article className="catalog-card"><div className="cover">{c.cover?<img src={c.cover} alt=""/>:<div className="cover-placeholder"><div><small>İREM COMFORT</small><h3 className="serif">{c.title}</h3><span>{c.year} · {c.season}</span></div></div>}</div><div className="card-body"><div className="eyebrow">{c.year} · {c.season}</div><h3>{c.title}</h3><div className="meta">{c.pages} sayfa · {c.collections.length} koleksiyon</div><div>{(c.tags||[]).slice(0,3).map(t=><span className="tag" key={t}>{t}</span>)}</div></div></article></Link>}
